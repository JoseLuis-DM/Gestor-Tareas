spring:
  application:
    name: gestor-tareas
  datasource:
    url: jdbc:mysql://localhost:3306/gestorbd
    username: root
    password: teti1402
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect