spring:
  application:
    name: gestor-tareas
  datasource:
    url: jdbc:postgresql://localhost:5432/gestorbd
    username: postgres
    password: pasword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true